# Dependencies & local runtime
node_modules/
.convex/
.DS_Store

# Secrets — never commit
.env
.env.local
.env.*.local
.cursor/skills/**/secrets.local.env
.cursor/skills/**/secrets.env

# Uploads & archives
public/uploads/
*.zip

# Runtime analytics snapshots (persistent local data, never commit)
data/*.json

# Editor noise
.cursor/*.log
